# Nixpacks (Railway) — use venv Python (Nix's `python` has no pip). "..." keeps the provider defaults.
[phases.install]
cmds = [
  "/opt/venv/bin/python -m pip install --upgrade pip setuptools wheel",
  "...",
]
