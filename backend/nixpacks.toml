# Nixpacks (Railway) — ensure setuptools + full requirements install (fixes pkg_resources on Python 3.12)
[phases.install]
cmds = [
  "python -m pip install --upgrade pip setuptools wheel",
  "python -m pip install -r requirements.txt",
]
